{% extends 'base.html.twig' %}

{% block title %}Acreditacion index{% endblock %}

{% block body %}
    <h1>Acreditacion index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for acreditacion in acreditacions %}
            <tr>
                <td>{{ acreditacion.id }}</td>
                <td>
                    <a href="{{ path('acreditacion_acreditacion_show', {'id': acreditacion.id}) }}">show</a>
                    <a href="{{ path('acreditacion_acreditacion_edit', {'id': acreditacion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('acreditacion_acreditacion_new') }}">Create new</a>
{% endblock %}
