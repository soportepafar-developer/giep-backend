{% extends 'base.html.twig' %}

{% block title %}Acreditacion{% endblock %}

{% block body %}
    <h1>Acreditacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ acreditacion.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('acreditacion_acreditacion_index') }}">back to list</a>

    <a href="{{ path('acreditacion_acreditacion_edit', {'id': acreditacion.id}) }}">edit</a>

    {{ include('acreditacion_acreditacion/_delete_form.html.twig') }}
{% endblock %}
