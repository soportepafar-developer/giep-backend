<form method="post" action="{{ path('acreditacion_tipo_item_delete', {'id': tipo_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_item.id) }}">
    <button class="btn">Delete</button>
</form>
